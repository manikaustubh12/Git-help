
git init //

git status // to see the changes

git commit -m “write any msg u want”

git add // add specific list of file

git add --all //add all files of the direcctory

git remote add origin https://github.com/manikaustubh12/

git branch // to see in which branch

git push -u origin master // to push the files

// to create the new branch 
git branch -c new_branch _name

// to switch into the other branch 
git branch -M branch_name
